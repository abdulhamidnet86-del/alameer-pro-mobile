import { MaterialCommunityIcons } from "@expo/vector-icons";
import * as DocumentPicker from "expo-document-picker";
import * as FileSystem from "expo-file-system/legacy";
import * as Print from "expo-print";
import * as Sharing from "expo-sharing";
import { captureRef } from "react-native-view-shot";
import { router } from "expo-router";
import { useEffect, useMemo, useRef, useState } from "react";
import { Alert, Image, PanResponder, Platform, ScrollView, StyleSheet, Switch, Text, TextInput, TouchableOpacity, View } from "react-native";
import { ScreenContainer } from "@/components/screen-container";
import { PowerMenu } from "@/components/power-menu";
import { useColors } from "@/hooks/use-colors";
import { useThemeContext } from "@/lib/theme-provider";
import { useRouterConnection } from "@/lib/router-context";
import { trpc } from "@/lib/trpc";
import { toArabicError } from "@/lib/arabic-errors";

type Design = { designKey: string; name: string; category: string; payload: CardSettings; updatedAt?: string | Date };
type CardElement = { visible: boolean; text: string; x: number; y: number; fontSize: number; color: string; bold: boolean; italic: boolean };
type CardSettings = { profileKind: "usermanager" | "hotspot" | "other"; profile: string; category: string; usernameDigits: number; columns: number; rows: number; cardsPerPage: number; autoA4: boolean; marginHorizontal: number; marginVertical: number; note: string; backgroundUri?: string; backgroundBase64?: string; backgroundMime?: string; backgroundEnabled: boolean; frameEnabled: boolean; frameColor: string; frameSize: number; usernameVisible: boolean; usernameX: number; usernameY: number; usernameFontSize: number; usernameColor: string; usernameBold: boolean; usernameLabel: string; titleVisible: boolean; titleText: string; titleX: number; titleY: number; titleFontSize: number; pageNumberVisible: boolean; pageNumberX: number; pageNumberY: number; pageNumberFontSize: number; logoEnabled: boolean; logoUri?: string; logoBase64?: string; logoMime?: string; logoX: number; logoY: number; logoWidth: number; logoHeight: number; password: CardElement; payment: CardElement; serial: CardElement; price: CardElement; pos: CardElement; barcodeEnabled: boolean; barcodeWidth: number; barcodeHeight: number; barcodeX: number; barcodeY: number; }; 
const element = (overrides: Partial<CardElement> = {}): CardElement => ({ visible: false, text: "", x: 6, y: 20, fontSize: 9, color: "#111827", bold: false, italic: false, ...overrides });
const elementKeys = ["password", "payment", "serial", "price", "pos"] as const;
const DEFAULT: CardSettings = { profileKind: "usermanager", profile: "", category: "عام", usernameDigits: 10, columns: 3, rows: 17, cardsPerPage: 51, autoA4: true, marginHorizontal: 0, marginVertical: 0, note: "", backgroundEnabled: true, frameEnabled: true, frameColor: "#000000", frameSize: 0.35, usernameVisible: true, usernameX: 6, usernameY: 8, usernameFontSize: 10, usernameColor: "#111827", usernameBold: true, usernameLabel: "اسم المستخدم", titleVisible: false, titleText: "", titleX: 6, titleY: 20, titleFontSize: 10, pageNumberVisible: false, pageNumberX: 6, pageNumberY: 30, pageNumberFontSize: 10, logoEnabled: false, logoX: 0, logoY: 0, logoWidth: 10, logoHeight: 10, password: element({ x: 6, y: 14, text: "{password}" }), payment: element({ x: 6, y: 22, text: "{payment}" }), serial: element({ x: 6, y: 30, text: "{serial}" }), price: element({ x: 6, y: 38, text: "{price}" }), pos: element({ x: 6, y: 46, text: "{pos}" }), barcodeEnabled: false, barcodeWidth: 18, barcodeHeight: 8, barcodeX: 44, barcodeY: 3 };

export default function CardsScreen() {
  const colors = useColors(); const { colorScheme, setColorScheme } = useThemeContext(); const { connection } = useRouterConnection(); const cardPreviewRef = useRef<View>(null); const previewSize = useRef({ width: 1, height: 1 }); const [settings, setSettings] = useState<CardSettings>(DEFAULT); const settingsRef = useRef(settings); const dragOrigin = useRef({ x: 0, y: 0 }); const [draggingUsername, setDraggingUsername] = useState(false); useEffect(() => { settingsRef.current = settings; }, [settings]); const [designs, setDesigns] = useState<Design[]>([]); const [backgroundFallback, setBackgroundFallback] = useState(false); const [designName, setDesignName] = useState("Default_Design"); const [category, setCategory] = useState("عام"); const [showBackgrounds, setShowBackgrounds] = useState(false); const [powerVisible, setPowerVisible] = useState(false);
  const designQuery = trpc.routeros.cards.list.useQuery(connection as any, { enabled: Boolean(connection), retry: false });
  const packages = trpc.routeros.packages.list.useQuery(connection as any, { enabled: Boolean(connection), retry: false });
  const hotspotProfilesQuery = trpc.routeros.list.useQuery({ connection: connection as any, resource: "hotspot.profiles", proplist: "name" } as any, { enabled: Boolean(connection), retry: false });
  const usersQuery = trpc.routeros.list.useQuery({ connection: connection as any, resource: settings.profileKind === "hotspot" ? "hotspot.users" : "usermanager.users", proplist: "username,password,profile,name" } as any, { enabled: Boolean(connection), retry: false });
  const save = trpc.routeros.cards.save.useMutation({ onSuccess: (data) => Alert.alert("تم الحفظ", data.persisted ? "حُفظ التصميم والخلفية على الخادم." : "حُفظ التصميم محليًا مؤقتًا لأن قاعدة البيانات غير متاحة."), onError: (error) => Alert.alert("تعذر الحفظ", toArabicError(error, "تعذر حفظ التصميم الآن. تحقق من اتصال الراوتر ثم أعد المحاولة.")) });
  const remove = trpc.routeros.cards.remove.useMutation({ onSuccess: () => { setDesigns((current) => current.filter((item) => item.designKey !== designKey)); Alert.alert("تم الحذف", "حُذف قالب التصميم من الخادم."); } });
  const profiles = useMemo(() => (packages.data?.profiles || []).map((item: any) => typeof item === "string" ? item : String(item.name || item["name"] || item)), [packages.data]);
  const hotspotProfiles = useMemo(() => (hotspotProfilesQuery.data || []).map((item: any) => String(item.name || item["name"] || item)), [hotspotProfilesQuery.data]);
  useEffect(() => { if (designQuery.data) setDesigns(designQuery.data as Design[]); }, [designQuery.data]);
  const designKey = useMemo(() => `design-${Array.from(designName.trim()).reduce((sum, char) => sum + char.charCodeAt(0), 0)}`,[designName]);
  const backgroundDataUri = settings.backgroundBase64 ? `data:${settings.backgroundMime || "image/png"};base64,${settings.backgroundBase64}` : undefined; const logoDataUri = settings.logoBase64 ? `data:${settings.logoMime || "image/png"};base64,${settings.logoBase64}` : undefined; const backgroundSource = !backgroundFallback && settings.backgroundUri ? settings.backgroundUri : backgroundDataUri;
  const patch = <K extends keyof CardSettings>(key: K, value: CardSettings[K]) => setSettings((current) => ({ ...current, [key]: value }));
  const step = (key: "columns" | "rows" | "cardsPerPage" | "usernameX" | "usernameY" | "usernameFontSize" | "titleX" | "titleY" | "pageNumberX" | "pageNumberY" | "frameSize" | "marginHorizontal" | "marginVertical", delta: number) => setSettings((current) => ({ ...current, [key]: Math.max(key === "columns" || key === "rows" || key === "cardsPerPage" ? 1 : 0, Number((current[key] + delta).toFixed(2))) }));
  const pickBackground = async () => { const result = await DocumentPicker.getDocumentAsync({ type: "image/*", copyToCacheDirectory: true }); if (result.canceled) return; const asset = result.assets[0]; try { const base64 = asset.base64 || await FileSystem.readAsStringAsync(asset.uri, { encoding: FileSystem.EncodingType.Base64 }); let permanentUri = asset.uri; if (FileSystem.documentDirectory) { const extension = (asset.name.split(".").pop() || "png").replace(/[^a-z0-9]/gi, "").toLowerCase() || "png"; const target = `${FileSystem.documentDirectory}card-background-${Date.now()}.${extension}`; await FileSystem.copyAsync({ from: asset.uri, to: target }); permanentUri = target; } patch("backgroundUri", permanentUri); patch("backgroundBase64", base64); patch("backgroundMime", asset.mimeType || "image/png"); patch("backgroundEnabled", true); setBackgroundFallback(false); setShowBackgrounds(false); } catch (error) { Alert.alert("تعذر حفظ الخلفية", toArabicError(error, "تعذر قراءة ملف الصورة من الهاتف. اختر صورة أخرى ثم أعد المحاولة.")); } };
  const pickLogo = async () => { const result = await DocumentPicker.getDocumentAsync({ type: "image/*", copyToCacheDirectory: true }); if (result.canceled) return; const asset = result.assets[0]; try { const base64 = asset.base64 || await FileSystem.readAsStringAsync(asset.uri, { encoding: FileSystem.EncodingType.Base64 }); let permanentUri = asset.uri; if (FileSystem.documentDirectory) { const extension = (asset.name.split(".").pop() || "png").replace(/[^a-z0-9]/gi, "").toLowerCase() || "png"; const target = `${FileSystem.documentDirectory}card-logo-${Date.now()}.${extension}`; await FileSystem.copyAsync({ from: asset.uri, to: target }); permanentUri = target; } patch("logoUri", permanentUri); patch("logoBase64", base64); patch("logoMime", asset.mimeType || "image/png"); patch("logoEnabled", true); } catch (error) { Alert.alert("تعذر حفظ الشعار", toArabicError(error, "تعذر قراءة ملف الشعار من الهاتف.")); } };
  const updateElement = (key: typeof elementKeys[number], changes: Partial<CardElement>) => setSettings((current) => ({ ...current, [key]: { ...current[key], ...changes } }));
  const saveDesign = () => { if (!connection) return Alert.alert("الاتصال مطلوب", "اتصل بالراوتر قبل حفظ التصميم وربطه ببياناته."); if (!designName.trim()) return Alert.alert("اسم التصميم مطلوب", "أدخل اسمًا واضحًا للقالب."); save.mutate({ connection, design: { designKey, name: designName.trim(), category: category.trim() || "عام", payload: JSON.stringify(settings) } }); };
  const loadDesign = (design: Design) => { setDesignName(design.name); setCategory(design.category); setBackgroundFallback(false); setSettings({ ...DEFAULT, ...design.payload }); };
  const deleteDesign = () => { if (!connection) return Alert.alert("الاتصال مطلوب", "اتصل بالراوتر أولًا."); Alert.alert("حذف التصميم", `هل تريد حذف ${designName}؟`, [{ text: "إلغاء", style: "cancel" }, { text: "حذف", style: "destructive", onPress: () => remove.mutate({ connection, designKey }) }]); };
  const printPreview = async () => { const realUsers = (usersQuery.data || []) as Record<string, string>[]; const printableUsers = realUsers.length ? realUsers : [{ username: "لا توجد بيانات مستخدمين" }]; const printableCount = Math.max(1, settings.autoA4 ? settings.columns * settings.rows : settings.cardsPerPage); const printRows = Math.max(1, settings.autoA4 ? settings.rows : Math.ceil(settings.cardsPerPage / Math.max(1, settings.columns))); const bg = settings.backgroundEnabled && settings.backgroundBase64 ? `data:${settings.backgroundMime || "image/png"};base64,${settings.backgroundBase64}` : ""; const logo = settings.logoEnabled ? (logoDataUri || settings.logoUri || "") : ""; const usernameStyle = `left:${settings.usernameX}%;top:${settings.usernameY}%;font-size:${settings.usernameFontSize}px;color:${settings.usernameColor};font-weight:${settings.usernameBold ? 800 : 400};`; const elementStyle = (item: CardElement) => `left:${item.x}%;top:${item.y}%;font-size:${item.fontSize}px;color:${item.color};font-weight:${item.bold ? 800 : 400};font-style:${item.italic ? "italic" : "normal"};`; const html = `<html dir="rtl"><head><meta charset="utf-8"><style>@page{size:210mm 297mm;margin:0}body{margin:0;width:210mm;height:297mm;font-family:Arial;background:#fff}.grid{display:grid;grid-template-columns:repeat(${settings.columns},minmax(0,1fr));grid-template-rows:repeat(${printRows},minmax(0,1fr));grid-auto-flow:row;gap:2mm;height:calc(297mm - ${settings.marginVertical * 2}mm);padding:${settings.marginVertical}mm ${settings.marginHorizontal}mm;box-sizing:border-box}.card{min-width:0;min-height:0;position:relative;overflow:hidden;border:${settings.frameEnabled ? settings.frameSize : 0}mm solid ${settings.frameColor};border-radius:0;background-color:#fff;background-image:url('${bg}');background-position:center;background-size:cover;background-repeat:no-repeat;box-sizing:border-box}.username{position:absolute;${usernameStyle};padding:1mm 2mm;border-radius:1mm;background:#ffffffaa}.title{position:absolute;left:${settings.titleX}%;top:${settings.titleY}%;font-size:${settings.titleFontSize}px}.extra{position:absolute;white-space:nowrap}.logo{position:absolute;left:${settings.logoX}%;top:${settings.logoY}%;width:${settings.logoWidth}%;height:${settings.logoHeight}%;object-fit:contain}.barcode{position:absolute;overflow:hidden;background:repeating-linear-gradient(90deg,#111 0 1px,#fff 1px 3px);font-size:0}.page{position:absolute;left:${settings.pageNumberX}%;top:${settings.pageNumberY}%;font-size:${settings.pageNumberFontSize}px}</style></head><body><div class="grid">${Array.from({ length: printableCount }, (_, index) => { const user = printableUsers[index % printableUsers.length]; const username = user.username || user.name || "غير معروف"; const valueFor = (key: keyof CardSettings) => { const raw = user[String(key)] || settings[key as keyof CardSettings]; return typeof raw === "string" || typeof raw === "number" ? String(raw) : ""; }; const extras = elementKeys.map((key) => settings[key].visible ? `<span class="extra" style="${elementStyle(settings[key])}">${settings[key].text.replace(/\{(\w+)\}/g, (_, token) => valueFor(token as keyof CardSettings))}</span>` : "").join(""); return `<div class="card">${settings.usernameVisible ? `<span class="username">${username}</span>` : ""}${settings.titleVisible ? `<span class="title">${settings.titleText}</span>` : ""}${extras}${logo ? `<img class="logo" src="${logo}" />` : ""}${settings.barcodeEnabled ? `<div class="barcode" style="left:${settings.barcodeX}%;top:${settings.barcodeY}%;width:${settings.barcodeWidth}%;height:${settings.barcodeHeight}%">${username}</div>` : ""}${settings.pageNumberVisible ? `<span class="page">${index + 1}</span>` : ""}</div>`; }).join("")}</div></body></html>`; try { if (Platform.OS === "web") { const popup = window.open("", "_blank", "width=1024,height=768"); if (!popup) throw new Error("يرجى السماح بفتح نافذة المعاينة"); popup.document.open(); popup.document.write(html); popup.document.close(); popup.focus(); window.setTimeout(() => { popup.print(); popup.close(); }, 250); } else { const file = await Print.printToFileAsync({ html }); if (await Sharing.isAvailableAsync()) await Sharing.shareAsync(file.uri, { mimeType: "application/pdf", dialogTitle: "مشاركة ملف كروت RouterOS" }); else await Print.printAsync({ html }); } } catch (error) { Alert.alert("تعذر إنشاء PDF", toArabicError(error, "تعذر إنشاء ملف PDF. تحقق من دعم الطباعة والمشاركة على الجهاز.")); } };
  const exportImage = async (format: "png" | "jpg") => { if (!cardPreviewRef.current) return; try { const uri = await captureRef(cardPreviewRef, { format, quality: 1, width: 1600, height: 1000 }); if (await Sharing.isAvailableAsync()) await Sharing.shareAsync(uri, { mimeType: format === "png" ? "image/png" : "image/jpeg", dialogTitle: `تصدير الكرت بصيغة ${format.toUpperCase()}` }); else Alert.alert("تم إنشاء الصورة", uri); } catch (error) { Alert.alert("تعذر تصدير الصورة", toArabicError(error, "تعذر تصدير الصورة. تحقق من صلاحية مشاركة الملفات على الجهاز.")); } };
  const moveUser = (dx: number, dy: number) => setSettings((current) => ({ ...current, usernameX: Math.max(0, Math.min(90, Number((current.usernameX + dx).toFixed(1)))), usernameY: Math.max(0, Math.min(90, Number((current.usernameY + dy).toFixed(1)))) }));
  const usernamePanResponder = useMemo(() => PanResponder.create({ onStartShouldSetPanResponder: () => true, onPanResponderGrant: () => { dragOrigin.current = { x: settingsRef.current.usernameX, y: settingsRef.current.usernameY }; setDraggingUsername(true); }, onPanResponderMove: (_event, gesture) => { const width = Math.max(1, previewSize.current.width); const height = Math.max(1, previewSize.current.height); const nextX = Math.max(0, Math.min(90, Number((dragOrigin.current.x - (gesture.dx / width) * 100).toFixed(1)))); const nextY = Math.max(0, Math.min(90, Number((dragOrigin.current.y + (gesture.dy / height) * 100).toFixed(1)))); setSettings((current) => ({ ...current, usernameX: nextX, usernameY: nextY })); }, onPanResponderRelease: () => setDraggingUsername(false), onPanResponderTerminate: () => setDraggingUsername(false) }), []);
  return <ScreenContainer edges={["top", "left", "right", "bottom"]} containerClassName={colorScheme === "dark" ? "bg-[#0E1D31]" : "bg-[#F6FBFC]"} className="px-3"><View style={styles.header}><View style={styles.headerActions}><Small icon="web" /><Small icon="weather-night" onPress={() => setColorScheme(colorScheme === "dark" ? "light" : "dark")} /><Small icon="logout" onPress={() => router.replace("/connect" as any)} /><Small icon="power" onPress={() => setPowerVisible(true)} /></View><View style={styles.brand}><Text style={[styles.brandText, { color: colors.foreground }]}>ALAMEER <Text style={styles.red}>PRO</Text></Text><Text style={[styles.caption, { color: colors.foreground }]}>تصاميم الكروت</Text></View><TouchableOpacity style={styles.back} onPress={() => router.back()}><MaterialCommunityIcons name="arrow-right" size={24} color="#123F95" /></TouchableOpacity></View>
    <View style={styles.body}><ScrollView style={styles.settingsScroll} contentContainerStyle={styles.content} showsVerticalScrollIndicator={false}>
      <Title icon="pencil-ruler" title="تصاميم الكروت" subtitle="تصميم وإدارة قوالب الكروت" />
      <TouchableOpacity style={styles.deleteButton} onPress={deleteDesign}><MaterialCommunityIcons name="delete" size={21} color="white" /><Text style={styles.whiteText}>حذف</Text></TouchableOpacity><TouchableOpacity style={styles.newButton} onPress={() => { setDesignName(`تصميم ${designs.length + 1}`); setSettings(DEFAULT); }}><MaterialCommunityIcons name="plus" size={22} color="#117E91" /><Text style={styles.tealText}>جديد</Text></TouchableOpacity><TouchableOpacity style={styles.saveButton} onPress={saveDesign}><MaterialCommunityIcons name="content-save" size={21} color="white" /><Text style={styles.whiteText}>حفظ التصميم</Text></TouchableOpacity>
      <Panel title="قالب التصميم" icon="lightbulb-outline"><Field label="اسم التصميم" value={designName} onChangeText={setDesignName} colors={colors} /><Text style={styles.label}>الفئة</Text><View style={styles.chips}>{["عام", "رمضان", "مميز", category].filter((item, index, arr) => arr.indexOf(item) === index).map((item) => <TouchableOpacity key={item} style={[styles.chip, category === item && styles.chipActive]} onPress={() => setCategory(item)}><Text style={category === item ? styles.chipTextActive : styles.chipText}>{item}</Text></TouchableOpacity>)}</View>{designs.length > 0 && <View style={styles.designList}>{designs.map((item) => <TouchableOpacity key={item.designKey} style={styles.designItem} onPress={() => loadDesign(item)}><MaterialCommunityIcons name="palette-outline" size={18} color="#1684CB" /><Text style={styles.designName}>{item.name} · {item.category}</Text></TouchableOpacity>)}</View>}</Panel>
      <Panel title="الربط بالباقة" icon="sync"><Text style={styles.label}>ربطه مع باقة User Manager</Text><SelectRow options={profiles} value={settings.profileKind === "usermanager" ? settings.profile : ""} onSelect={(value) => { patch("profileKind", "usermanager"); patch("profile", value); }} /><Text style={styles.hint}>{profiles.length ? "تم جلب الباقات الحقيقية من الراوتر المتصل." : "اتصل بالراوتر لجلب الباقات الحقيقية."}</Text><Text style={styles.label}>ربطه مع باقة Hotspot</Text><SelectRow options={hotspotProfiles} value={settings.profileKind === "hotspot" ? settings.profile : ""} onSelect={(value) => { patch("profileKind", "hotspot"); patch("profile", value); }} /><TouchableOpacity style={styles.refresh} onPress={() => { void packages.refetch(); void hotspotProfilesQuery.refetch(); }}><MaterialCommunityIcons name="refresh" size={18} color="#1684CB" /><Text style={styles.refreshText}>{packages.isFetching || hotspotProfilesQuery.isFetching ? "جارٍ مزامنة الباقات..." : "تحديث الباقات من RouterOS"}</Text></TouchableOpacity>{packages.error ? <Text style={styles.errorHint}>{toArabicError(packages.error, "تعذر جلب باقات User Manager من الراوتر.")}</Text> : null}{hotspotProfilesQuery.error ? <Text style={styles.errorHint}>{toArabicError(hotspotProfilesQuery.error, "تعذر جلب باقات Hotspot من الراوتر.")}</Text> : null}</Panel>
      <Panel title="بيانات الصفحة" icon="tune-variant"><SectionTitle title="بيانات الطباعة" /><Stepper label="عدد الأعمدة" value={settings.columns} onMinus={() => step("columns", -1)} onPlus={() => step("columns", 1)} /><Stepper label="عدد الصفوف" value={settings.rows} onMinus={() => step("rows", -1)} onPlus={() => step("rows", 1)} /><Stepper label="عدد الكروت" value={settings.cardsPerPage} onMinus={() => step("cardsPerPage", -1)} onPlus={() => step("cardsPerPage", 1)} /><Stepper label="عدد أرقام اسم المستخدم" value={settings.usernameDigits} onMinus={() => patch("usernameDigits", Math.max(4, settings.usernameDigits - 1))} onPlus={() => patch("usernameDigits", Math.min(16, settings.usernameDigits + 1))} /><Toggle label="مضاعفة الطباعة لمقاس الكروت حسب A4" value={settings.autoA4} onChange={(value) => patch("autoA4", value)} /><SectionTitle title="هامش بين الكروت" /><InlineStepper label="أفقي (مم)" value={settings.marginHorizontal} onMinus={() => step("marginHorizontal", -0.5)} onPlus={() => step("marginHorizontal", 0.5)} /><InlineStepper label="رأسي (مم)" value={settings.marginVertical} onMinus={() => step("marginVertical", -0.5)} onPlus={() => step("marginVertical", 0.5)} /><Field label="ملاحظة على الصفحة" value={settings.note} onChangeText={(value) => patch("note", value)} colors={colors} multiline /></Panel>
      <Panel title="الخلفية" icon="image-outline"><Toggle label="تفعيل خلفية الكرت" value={settings.backgroundEnabled} onChange={(value) => patch("backgroundEnabled", value)} /><TouchableOpacity style={styles.clearButton} onPress={() => { patch("backgroundUri", undefined); patch("backgroundBase64", undefined); patch("backgroundMime", undefined); setBackgroundFallback(false); }}><MaterialCommunityIcons name="close" size={18} color="#19A982" /><Text style={styles.greenText}>مسح</Text></TouchableOpacity><TouchableOpacity style={styles.imageButton} onPress={pickBackground}><MaterialCommunityIcons name="image-plus" size={19} color="#126FBA" /><Text style={styles.imageButtonText}>اختيار صورة ورفعها</Text></TouchableOpacity>{backgroundSource && <Image source={{ uri: backgroundSource }} style={styles.bgThumb} resizeMode="cover" onError={() => setBackgroundFallback(true)} />}<Text style={styles.hint}>تُحفظ الخلفية ضمن التصميم على الخادم، وتُملأ مساحة الكرت بدون حواف إضافية.</Text></Panel>
      <Panel title="حواف الكرت" icon="border-all"><Toggle label="تفعيل الإطار" value={settings.frameEnabled} onChange={(value) => patch("frameEnabled", value)} /><ColorRow label="لون الإطار" value={settings.frameColor} onChange={(value) => patch("frameColor", value)} /><InlineStepper label="حجم الإطار (مم)" value={settings.frameSize} onMinus={() => step("frameSize", -0.05)} onPlus={() => step("frameSize", 0.05)} /></Panel>
      <Panel title="عناصر الكرت" icon="format-list-bulleted"><SectionTitle title="عرض الوحدة / العنوان" /><Toggle label="إظهار اسم المستخدم" value={settings.usernameVisible} onChange={(value) => patch("usernameVisible", value)} /><Toggle label="تفعيل عنوان مخصص" value={settings.titleVisible} onChange={(value) => patch("titleVisible", value)} /><Field label="نص العنوان" value={settings.titleText} onChangeText={(value) => patch("titleText", value)} colors={colors} /><SectionTitle title="إعدادات الخط" /><ColorRow label="لون الخط" value={settings.usernameColor} onChange={(value) => patch("usernameColor", value)} /><InlineStepper label="حجم الخط" value={settings.usernameFontSize} onMinus={() => step("usernameFontSize", -1)} onPlus={() => step("usernameFontSize", 1)} /><Toggle label="الخط غامق" value={settings.usernameBold} onChange={(value) => patch("usernameBold", value)} /><SectionTitle title="رقم الصفحة" /><Toggle label="إظهار رقم الصفحة" value={settings.pageNumberVisible} onChange={(value) => patch("pageNumberVisible", value)} /><PositionControls onMove={moveUser} label="تحريك كود المستخدم يدويًا" />{([ ["password", "كلمة المرور"], ["payment", "رقم الدفعة"], ["serial", "الرقم التسلسلي"], ["price", "سعر الباقة"], ["pos", "نقطة البيع"] ] as const).map(([key, label]) => <View key={key} style={styles.elementBox}><Toggle label={`إظهار ${label}`} value={settings[key].visible} onChange={(value) => updateElement(key, { visible: value })} /><Field label={`نص ${label}`} value={settings[key].text} onChangeText={(value) => updateElement(key, { text: value })} colors={colors} /><InlineStepper label={`حجم ${label}`} value={settings[key].fontSize} onMinus={() => updateElement(key, { fontSize: Math.max(4, settings[key].fontSize - 1) })} onPlus={() => updateElement(key, { fontSize: Math.min(72, settings[key].fontSize + 1) })} /><PositionControls onMove={(dx, dy) => updateElement(key, { x: Math.max(0, settings[key].x + dx), y: Math.max(0, settings[key].y + dy) })} label={`تحريك ${label}`} /></View>)}</Panel>
      <Panel title="الباركود" icon="barcode"><Toggle label="تفعيل الباركود" value={settings.barcodeEnabled} onChange={(value) => patch("barcodeEnabled", value)} />{settings.barcodeEnabled && <><InlineStepper label="عرض الباركود (مم)" value={settings.barcodeWidth} onMinus={() => patch("barcodeWidth", Math.max(5, settings.barcodeWidth - 0.5))} onPlus={() => patch("barcodeWidth", settings.barcodeWidth + 0.5)} /><InlineStepper label="ارتفاع الباركود (مم)" value={settings.barcodeHeight} onMinus={() => patch("barcodeHeight", Math.max(3, settings.barcodeHeight - 0.5))} onPlus={() => patch("barcodeHeight", settings.barcodeHeight + 0.5)} /><PositionControls onMove={(dx, dy) => { patch("barcodeX", Math.max(0, settings.barcodeX + dx)); patch("barcodeY", Math.max(0, settings.barcodeY + dy)); }} label="تحريك الباركود" /></>}</Panel>
      <Panel title="الشعار" icon="image"><Toggle label="تفعيل شعار الكرت" value={settings.logoEnabled} onChange={(value) => patch("logoEnabled", value)} /><TouchableOpacity style={styles.imageButton} onPress={pickLogo}><MaterialCommunityIcons name="image-plus" size={19} color="#126FBA" /><Text style={styles.imageButtonText}>اختيار شعار</Text></TouchableOpacity>{settings.logoUri && <Image source={{ uri: settings.logoUri }} style={styles.logoThumb} resizeMode="contain" />}<View style={styles.logoGrid}><InlineStepper label="العرض (مم)" value={settings.logoWidth} onMinus={() => patch("logoWidth", Math.max(2, settings.logoWidth - 0.5))} onPlus={() => patch("logoWidth", settings.logoWidth + 0.5)} /><InlineStepper label="الارتفاع (مم)" value={settings.logoHeight} onMinus={() => patch("logoHeight", Math.max(2, settings.logoHeight - 0.5))} onPlus={() => patch("logoHeight", settings.logoHeight + 0.5)} /></View></Panel>
      <Panel title="تصنيفات الخلفيات" icon="folder-multiple-image"><Text style={styles.hint}>احفظ الخلفية داخل قالب باسم وفئة، ثم أعد اختياره من قائمة القوالب في أي وقت.</Text><TouchableOpacity style={styles.imageButton} onPress={() => setShowBackgrounds((value) => !value)}><MaterialCommunityIcons name="folder-star-outline" size={19} color="#126FBA" /><Text style={styles.imageButtonText}>{showBackgrounds ? "إخفاء الخلفيات المحفوظة" : "عرض الخلفيات المحفوظة"}</Text></TouchableOpacity>{showBackgrounds && designs.filter((item) => item.payload.backgroundUri || item.payload.backgroundBase64).map((item) => <TouchableOpacity key={item.designKey} style={styles.designItem} onPress={() => loadDesign(item)}><MaterialCommunityIcons name="image-outline" size={18} color="#159B68" /><Text style={styles.designName}>{item.name} · {item.category}</Text></TouchableOpacity>)}</Panel>
      <View style={{ height: 250 }} />
    </ScrollView><View style={[styles.previewDock, { backgroundColor: colors.surface, borderColor: colors.border }]}><View style={styles.previewHeader}><Text style={[styles.previewTitle, { color: colors.foreground }]}>معاينة الكرت</Text><View style={styles.previewLabel}><MaterialCommunityIcons name="eye-outline" size={17} color="#1684CB" /><Text style={styles.previewLabelText}>معاينة ثابتة</Text></View></View><View ref={cardPreviewRef} collapsable={false} onLayout={(event) => { previewSize.current = { width: event.nativeEvent.layout.width, height: event.nativeEvent.layout.height }; }} style={styles.previewCard}>{settings.backgroundEnabled && backgroundSource ? <Image source={{ uri: backgroundSource }} style={StyleSheet.absoluteFillObject} resizeMode="cover" onError={() => setBackgroundFallback(true)} /> : <View style={styles.previewWave} />}{settings.frameEnabled && <View style={[styles.previewFrame, { borderColor: settings.frameColor, borderWidth: Math.max(1, settings.frameSize * 2) }]} />}{settings.usernameVisible && <Text {...usernamePanResponder.panHandlers} style={[styles.previewUsername, draggingUsername && styles.previewUsernameDragging, { right: `${Math.min(90, settings.usernameX)}%`, top: `${Math.min(90, settings.usernameY)}%`, fontSize: Math.max(8, settings.usernameFontSize), color: settings.usernameColor, fontWeight: settings.usernameBold ? "800" : "400" }]}>{"1234567890123456".slice(0, settings.usernameDigits)}</Text>}{settings.titleVisible && <Text style={[styles.previewTitleText, { left: `${settings.titleX}%`, top: `${settings.titleY}%`, fontSize: settings.titleFontSize }]}>{settings.titleText}</Text>}{settings.logoEnabled && (settings.logoUri || logoDataUri) && <Image source={{ uri: logoDataUri || settings.logoUri }} style={{ position: "absolute", left: `${settings.logoX}%`, top: `${settings.logoY}%`, width: `${Math.max(8, settings.logoWidth)}%`, height: `${Math.max(8, settings.logoHeight)}%` }} resizeMode="contain" />}{elementKeys.map((key) => settings[key].visible && <Text key={key} style={{ position: "absolute", left: `${Math.min(90, settings[key].x)}%`, top: `${Math.min(90, settings[key].y)}%`, fontSize: Math.max(7, settings[key].fontSize), color: settings[key].color, fontWeight: settings[key].bold ? "800" : "400", fontStyle: settings[key].italic ? "italic" : "normal" }}>{settings[key].text || `{${key}}`}</Text>)}</View><TouchableOpacity style={styles.previewButton} onPress={printPreview}><MaterialCommunityIcons name="file-pdf-box" size={20} color="white" /><Text style={styles.whiteText}>معاينة وطباعة PDF</Text></TouchableOpacity><View style={styles.exportRow}><TouchableOpacity style={styles.exportButton} onPress={() => exportImage("png")}><MaterialCommunityIcons name="file-image" size={18} color="#1684CB" /><Text style={styles.exportText}>PNG عالي الدقة</Text></TouchableOpacity><TouchableOpacity style={styles.exportButton} onPress={() => exportImage("jpg")}><MaterialCommunityIcons name="image" size={18} color="#1684CB" /><Text style={styles.exportText}>JPG عالي الدقة</Text></TouchableOpacity></View></View></View><PowerMenu visible={powerVisible} onClose={() => setPowerVisible(false)} /></ScreenContainer>;
}
function Title({ icon, title, subtitle }: { icon: keyof typeof MaterialCommunityIcons.glyphMap; title: string; subtitle: string }) { return <View style={styles.titleRow}><View style={styles.titleIcon}><MaterialCommunityIcons name={icon} size={24} color="#19A982" /></View><View><Text style={styles.titleText}>{title}</Text><Text style={styles.subtitle}>{subtitle}</Text></View></View>; }
function Panel({ title, icon, children }: { title: string; icon: keyof typeof MaterialCommunityIcons.glyphMap; children: React.ReactNode }) { return <View style={styles.panel}><View style={styles.panelTitle}><Text style={styles.panelText}>{title}</Text><MaterialCommunityIcons name={icon} size={20} color="#1684CB" /></View>{children}</View>; }
function SectionTitle({ title }: { title: string }) { return <View style={styles.sectionTitle}><View style={styles.tealDot} /><Text style={styles.sectionTitleText}>{title}</Text></View>; }
function Field({ label, value, onChangeText, colors, multiline }: { label: string; value: string; onChangeText: (value: string) => void; colors: ReturnType<typeof useColors>; multiline?: boolean }) { return <View style={styles.field}><Text style={[styles.label, { color: colors.muted }]}>{label}</Text><TextInput value={value} onChangeText={onChangeText} multiline={multiline} placeholderTextColor="#99A6B6" style={[styles.fieldInput, { color: colors.foreground, borderColor: colors.border, minHeight: multiline ? 65 : 40 }]} /></View>; }
function Toggle({ label, value, onChange }: { label: string; value: boolean; onChange: (value: boolean) => void }) { return <View style={styles.toggle}><Text style={styles.label}>{label}</Text><Switch value={value} onValueChange={onChange} trackColor={{ false: "#A9B4C2", true: "#159B68" }} thumbColor="white" /></View>; }
function Stepper({ label, value, onMinus, onPlus }: { label: string; value: number; onMinus: () => void; onPlus: () => void }) { return <View style={styles.stepper}><Text style={styles.label}>{label}</Text><View style={styles.stepActions}><MiniButton icon="plus" onPress={onPlus} /><Text style={styles.value}>{value}</Text><MiniButton icon="minus" red onPress={onMinus} /></View></View>; }
function InlineStepper({ label, value, onMinus, onPlus }: { label: string; value: number; onMinus: () => void; onPlus: () => void }) { return <View style={styles.inlineStepper}><Text style={styles.label}>{label}</Text><View style={styles.stepActions}><MiniButton icon="plus" onPress={onPlus} /><Text style={styles.value}>{Number(value).toFixed(1)}</Text><MiniButton icon="minus" red onPress={onMinus} /></View></View>; }
function MiniButton({ icon, onPress, red }: { icon: "plus" | "minus"; onPress: () => void; red?: boolean }) { return <TouchableOpacity style={[styles.mini, red && styles.miniRed]} onPress={onPress}><MaterialCommunityIcons name={icon} size={17} color={red ? "#D33D4A" : "#159B68"} /></TouchableOpacity>; }
function SelectRow({ options, value, onSelect }: { options: string[]; value: string; onSelect: (value: string) => void }) { return <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.selectRow}>{(options.length ? options : ["لا توجد باقات محملة"]).map((option) => <TouchableOpacity key={option} style={[styles.selectChip, value === option && styles.selectChipActive]} onPress={() => options.length && onSelect(option)}><Text style={value === option ? styles.chipTextActive : styles.chipText}>{value === option ? option : options.length ? option : "اختر"}</Text><MaterialCommunityIcons name="chevron-down" size={16} color="#536273" /></TouchableOpacity>)}</ScrollView>; }
function ColorRow({ label, value, onChange }: { label: string; value: string; onChange: (value: string) => void }) { return <View style={styles.colorRow}><Text style={styles.label}>{label}</Text><TextInput value={value} onChangeText={onChange} style={styles.colorInput} autoCapitalize="characters" /><View style={[styles.colorSwatch, { backgroundColor: value }]} /></View>; }
function PositionControls({ onMove, label }: { onMove: (dx: number, dy: number) => void; label: string }) { return <View style={styles.position}><Text style={styles.label}>{label}</Text><View style={styles.arrows}><TouchableOpacity style={styles.arrow} onPress={() => onMove(0, -0.5)}><MaterialCommunityIcons name="chevron-up" size={22} color="#159B68" /></TouchableOpacity><TouchableOpacity style={styles.arrow} onPress={() => onMove(0, 0.5)}><MaterialCommunityIcons name="chevron-down" size={22} color="#159B68" /></TouchableOpacity><TouchableOpacity style={styles.arrow} onPress={() => onMove(-0.5, 0)}><MaterialCommunityIcons name="chevron-left" size={22} color="#159B68" /></TouchableOpacity><TouchableOpacity style={styles.arrow} onPress={() => onMove(0.5, 0)}><MaterialCommunityIcons name="chevron-right" size={22} color="#159B68" /></TouchableOpacity></View></View>; }
function Small({ icon, onPress }: { icon: keyof typeof MaterialCommunityIcons.glyphMap; onPress?: () => void }) { return <TouchableOpacity style={styles.small} onPress={onPress}><MaterialCommunityIcons name={icon} size={18} color="#123F95" /></TouchableOpacity>; }
const styles = StyleSheet.create({ header: { height: 58, borderRadius: 18, backgroundColor: "#FFFFFF", borderWidth: 1, borderColor: "#DCE7F0", flexDirection: "row-reverse", alignItems: "center", justifyContent: "space-between", padding: 7 }, headerActions: { flexDirection: "row", gap: 4 }, small: { width: 29, height: 29, borderRadius: 10, backgroundColor: "#F4F8FF", alignItems: "center", justifyContent: "center" }, brand: { alignItems: "center" }, brandText: { fontFamily: "CairoExtraBold", fontSize: 15 }, red: { color: "#DF1D36" }, caption: { fontFamily: "CairoExtraBold", fontSize: 10 }, back: { width: 40, height: 40, borderRadius: 13, backgroundColor: "#F4F8FF", alignItems: "center", justifyContent: "center" }, body: { flex: 1 }, settingsScroll: { flex: 1 }, content: { paddingVertical: 12, gap: 10, paddingBottom: 12 }, titleRow: { flexDirection: "row-reverse", alignItems: "center", gap: 10, paddingHorizontal: 4 }, titleIcon: { width: 45, height: 45, borderRadius: 14, backgroundColor: "#E9F8F3", alignItems: "center", justifyContent: "center" }, titleText: { fontFamily: "CairoExtraBold", fontSize: 20, color: "#182A42", textAlign: "right" }, subtitle: { color: "#7A8798", fontSize: 10, textAlign: "right" }, panel: { borderRadius: 16, backgroundColor: "#FFFFFF", borderWidth: 1, borderColor: "#DAE7EA", padding: 12, gap: 9 }, panelTitle: { flexDirection: "row-reverse", alignItems: "center", justifyContent: "flex-start", gap: 7, borderBottomWidth: 1, borderBottomColor: "#E5EEF0", paddingBottom: 7 }, panelText: { color: "#11828E", fontFamily: "CairoExtraBold", fontSize: 14 }, deleteButton: { height: 44, borderRadius: 13, backgroundColor: "#EF1723", flexDirection: "row-reverse", gap: 8, alignItems: "center", justifyContent: "center" }, newButton: { height: 44, borderRadius: 13, borderWidth: 1, borderColor: "#D1E3E8", backgroundColor: "#FFFFFF", flexDirection: "row-reverse", gap: 8, alignItems: "center", justifyContent: "center" }, saveButton: { height: 44, borderRadius: 13, backgroundColor: "#087E9B", flexDirection: "row-reverse", gap: 8, alignItems: "center", justifyContent: "center" }, whiteText: { color: "#FFFFFF", fontFamily: "CairoExtraBold", fontSize: 12 }, tealText: { color: "#117E91", fontFamily: "CairoExtraBold", fontSize: 12 }, field: { gap: 4 }, label: { color: "#344052", textAlign: "right", fontSize: 10, fontFamily: "CairoExtraBold" }, fieldInput: { borderWidth: 1, borderRadius: 10, paddingHorizontal: 10, paddingVertical: 7, textAlign: "right", fontSize: 11, backgroundColor: "#FFFFFF" }, chips: { flexDirection: "row-reverse", gap: 6, flexWrap: "wrap" }, chip: { borderRadius: 10, paddingHorizontal: 11, paddingVertical: 7, backgroundColor: "#F0F5F7" }, chipActive: { backgroundColor: "#DDF4EC" }, chipText: { color: "#718093", fontSize: 10 }, chipTextActive: { color: "#087E69", fontFamily: "CairoExtraBold", fontSize: 10 }, designList: { gap: 5 }, designItem: { flexDirection: "row-reverse", alignItems: "center", gap: 6, padding: 9, borderRadius: 10, backgroundColor: "#F5FAFC" }, designName: { flex: 1, textAlign: "right", color: "#40516A", fontSize: 10 }, elementBox: { gap: 6, paddingTop: 6, borderTopWidth: 1, borderTopColor: "#E5EEF0" }, logoThumb: { height: 72, width: "100%", borderRadius: 10, backgroundColor: "#F5FAFC" }, logoGrid: { gap: 4 }, errorHint: { color: "#C6283D", fontSize: 9, textAlign: "right", lineHeight: 16 }, hint: { color: "#71829E", fontSize: 9, textAlign: "right", lineHeight: 16 }, refresh: { flexDirection: "row-reverse", alignItems: "center", justifyContent: "center", gap: 6, padding: 8, borderRadius: 10, backgroundColor: "#EDF7FC" }, refreshText: { color: "#1684CB", fontSize: 10, fontFamily: "CairoExtraBold" }, selectRow: { flexDirection: "row-reverse", gap: 7 }, selectChip: { minWidth: 145, height: 40, borderRadius: 10, borderWidth: 1, borderColor: "#DCE7EA", backgroundColor: "#FFFFFF", flexDirection: "row-reverse", alignItems: "center", justifyContent: "space-between", paddingHorizontal: 10 }, selectChipActive: { borderColor: "#159B68", backgroundColor: "#E7F6F0" }, stepper: { flexDirection: "row-reverse", justifyContent: "space-between", alignItems: "center", minHeight: 45 }, inlineStepper: { flexDirection: "row-reverse", justifyContent: "space-between", alignItems: "center", paddingVertical: 4 }, stepActions: { flexDirection: "row-reverse", alignItems: "center", gap: 6 }, mini: { width: 34, height: 34, borderRadius: 9, backgroundColor: "#DDF7E8", alignItems: "center", justifyContent: "center" }, miniRed: { backgroundColor: "#FCE5E8" }, value: { minWidth: 40, textAlign: "center", color: "#243244", fontFamily: "CairoExtraBold", fontSize: 12 }, toggle: { minHeight: 43, flexDirection: "row-reverse", justifyContent: "space-between", alignItems: "center" }, sectionTitle: { flexDirection: "row-reverse", alignItems: "center", gap: 6, marginTop: 5 }, tealDot: { width: 8, height: 8, borderRadius: 8, backgroundColor: "#078794" }, sectionTitleText: { color: "#167E8C", fontFamily: "CairoExtraBold", fontSize: 12 }, clearButton: { height: 40, borderRadius: 10, backgroundColor: "#BDE6DE", flexDirection: "row-reverse", gap: 7, alignItems: "center", justifyContent: "center" }, greenText: { color: "#15A982", fontFamily: "CairoExtraBold", fontSize: 11 }, imageButton: { height: 42, borderRadius: 10, backgroundColor: "#E6EEF9", flexDirection: "row-reverse", gap: 7, alignItems: "center", justifyContent: "center" }, imageButtonText: { color: "#126FBA", fontFamily: "CairoExtraBold", fontSize: 11 }, bgThumb: { height: 100, borderRadius: 10, width: "100%" }, colorRow: { minHeight: 45, flexDirection: "row-reverse", alignItems: "center", gap: 8 }, colorInput: { flex: 1, borderWidth: 1, borderColor: "#DCE5EC", borderRadius: 9, height: 38, paddingHorizontal: 8, textAlign: "center", color: "#213044", fontSize: 10 }, colorSwatch: { width: 34, height: 34, borderRadius: 8, borderWidth: 1, borderColor: "#CFD9E3" }, position: { gap: 7 }, arrows: { flexDirection: "row-reverse", justifyContent: "center", gap: 7 }, arrow: { width: 42, height: 38, borderRadius: 10, backgroundColor: "#E8F6F2", alignItems: "center", justifyContent: "center" }, arrowText: { color: "#159B68", fontSize: 23 }, previewDock: { position: "absolute", bottom: 0, left: 0, right: 0, padding: 10, borderRadius: 16, borderWidth: 1, shadowColor: "#0D7380", shadowOpacity: 0.16, shadowRadius: 15, elevation: 9 }, previewHeader: { flexDirection: "row-reverse", justifyContent: "space-between", alignItems: "center", marginBottom: 6 }, previewTitle: { fontFamily: "CairoExtraBold", fontSize: 13 }, previewLabel: { flexDirection: "row-reverse", alignItems: "center", gap: 4 }, previewLabelText: { color: "#1684CB", fontSize: 10, fontFamily: "CairoExtraBold" }, previewCard: { height: 82, width: "70%", alignSelf: "center", borderRadius: 10, overflow: "hidden", backgroundColor: "#FFFFFF", justifyContent: "center" }, previewWave: { ...StyleSheet.absoluteFillObject, backgroundColor: "#FFFFFF" }, previewFrame: { ...StyleSheet.absoluteFillObject, borderRadius: 10 }, previewUsername: { position: "absolute", paddingHorizontal: 6, paddingVertical: 2, backgroundColor: "#FFFFFFCC", borderRadius: 4, zIndex: 20 }, previewUsernameDragging: { backgroundColor: "#D9F8EF", borderWidth: 1, borderColor: "#159B68" }, previewTitleText: { position: "absolute", color: "#152238" }, previewButton: { height: 42, borderRadius: 12, backgroundColor: "#18B895", marginTop: 7, flexDirection: "row-reverse", alignItems: "center", justifyContent: "center", gap: 7 }, exportRow: { flexDirection: "row-reverse", gap: 7, marginTop: 6 }, exportButton: { flex: 1, height: 34, borderRadius: 9, backgroundColor: "#E8F3FC", flexDirection: "row-reverse", alignItems: "center", justifyContent: "center", gap: 5 }, exportText: { color: "#1684CB", fontFamily: "CairoExtraBold", fontSize: 9 } });
