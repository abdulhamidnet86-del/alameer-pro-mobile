# تصميم تطبيق ALAMEER PRO

## نطاق التطبيق

تطبيق جوال عربي باتجاه RTL لإدارة أجهزة MikroTik التي تعمل على RouterOS v6.49.19. الواجهة تحاكي الهوية البصرية المرجعية: خلفيات بيضاء أو كحلية، تدرجات أزرق/أحمر، بطاقات مستديرة، حدود خفيفة، عناوين عربية واضحة، وأزرار عالية التباين. البيانات المعروضة تأتي من الراوتر الفعلي فقط؛ عند غياب الاتصال تظهر حالات تحميل أو عدم اتصال أو خطأ، ولا يتم عرض أرقام بديلة.

## القاعدة المعمارية

يستخدم التطبيق طبقة وسيطة آمنة بدل فتح RouterOS API مباشرة داخل الهاتف. تتصل الطبقة الوسيطة بالراوتر عبر RouterOS API المتوافق مع الإصدار 6.49.19، بينما يتصل التطبيق بها عبر HTTPS. داخل الشبكة يمكن للوسيط الوصول إلى عنوان الراوتر المحلي، وخارج الشبكة يتم الوصول إلى الوسيط عبر نطاق HTTPS أو VPN/نفق خاص. لا تُحفظ كلمات مرور الراوتر في واجهة الهاتف ولا تُرسل إلى خدمة عامة غير موثوقة.

## الشاشات

| الشاشة | المحتوى والوظيفة |
|---|---|
| البداية | شعار ALAMEER PRO، حالة الاتصال، مؤشرات الإصدار، وزرا الوصول إلى بيانات الدخول أو الراوترات المسجلة. |
| بيانات الاتصال | اختيار نوع الاتصال، عنوان IP أو النطاق، المنفذ، اسم المستخدم، كلمة المرور، واسم اختياري للراوتر. زر اختبار الاتصال يعرض النتيجة الحقيقية فقط. |
| الراوترات المسجلة | قائمة الاتصالات المحفوظة مع حالة متصل/غير متصل، وت actions للتعديل والحذف والاتصال والاستيراد المشفر. |
| الرئيسية | ملخص حي من الراوتر: الإصدار، مدة التشغيل، المعالج، الذاكرة، المستخدمون النشطون، وإجماليات User Manager وHotspot إن كانت الصلاحيات تسمح. |
| User Manager | بطاقات الإجراءات السريعة: إدارة الكروت، إضافة كرت، إدارة الباقات، تقارير الدفعات، تقارير الأجهزة، قائمة الحظر، الصيانة، النسخ الاحتياطي، وإضافة خدمة. كل عنصر يستدعي عملية حقيقية على الراوتر أو يعرض عدم توفر المسار. |
| Hotspot | إدارة الكروت، إضافة كرت، إدارة الباقات، الكشف عن مستخدمي الهوتسبوت، تقارير الاستخدام، وخدمات إضافية. العناصر غير المفعّلة على الراوتر تظهر بحالة واضحة بدل محتوى تجريبي. |
| قائمة الكروت | بيانات حقيقية قابلة للبحث والتصفية، مع إنشاء/تعديل/تعطيل/حذف وفق الصلاحيات. |
| نموذج الكرت | حقول مرتبطة مباشرة بخصائص User Manager أو Hotspot، مع تحقق قبل الإرسال وتأكيد للعمليات الحساسة. |
| الملف والإعدادات | إدارة الراوتر الحالي، تحديث يدوي، مهلة الاتصال، تسجيل الخروج، واختيار المظهر. |

## التدفقات الأساسية

1. يفتح المستخدم التطبيق، فتُفحص حالة الجلسة المحلية دون افتراض اتصال.
2. يختار المستخدم إضافة راوتر، يدخل بيانات الاتصال، ثم يضغط اختبار الاتصال.
3. يرسل الوسيط طلبًا حقيقيًا إلى RouterOS API ويعيد إصدار الراوتر وصلاحية الحساب وحالة الاتصال.
4. بعد نجاح الاتصال ينتقل المستخدم إلى الرئيسية، حيث تُجلب البيانات المطلوبة على دفعات صغيرة مع زر تحديث يدوي.
5. يختار المستخدم User Manager أو Hotspot، ثم يفتح قائمة حقيقية أو إجراءً حقيقيًا.
6. قبل الإنشاء أو التعديل أو الحذف يعرض التطبيق ملخص العملية، ثم يرسلها بعد التأكيد، ويعيد تحميل المورد المتأثر.
7. عند انتهاء الجلسة أو انقطاع الراوتر، يحتفظ التطبيق ببيانات الاتصال المشفرة محليًا إن اختار المستخدم ذلك، لكن لا يعرض بيانات قديمة على أنها حية.

## نظام التصميم

تستخدم الواجهة خطًا عربيًا واضحًا من حزمة Expo المحلية أو خط النظام مع أوزان متدرجة، وتُحافظ على RTL في النصوص وترتيب الأزرار. اللون الأساسي هو الأزرق الملكي `#0647D8`، لون الحالة الناجحة `#16A34A`، الأحمر الإجرائي `#E31B35`، الأزرق الداكن للنصوص `#102A62`، والخلفية `#F8FAFF`. الأزرار الرئيسية تستخدم تدرجًا من الأزرق إلى الأحمر عند العمليات البارزة، بينما بطاقات الخدمات تستخدم تدرجات فاتحة مختلفة مع حدود دقيقة وظلال خفيفة.

كل شاشة مصممة للوضع الرأسي وللاستخدام بيد واحدة. يتم وضع الإجراء الأساسي في منطقة الوصول السفلية، وتستخدم القوائم FlatList، وتُعرض حالات التحميل داخل البطاقة نفسها. لا تُستخدم أيقونات نصية مرتجلة؛ تُستخدم أيقونات Expo المتاحة أو رموز موحدة من مكتبة الأيقونات.

## الأداء والاعتمادية

تُجلب الحقول الضرورية فقط من RouterOS، وتُنفذ الطلبات المتوازية المحدودة عبر الخادم مع مهلة وإلغاء عند مغادرة الشاشة. تعتمد القوائم على pagination أو حدود واضحة عندما يدعمها مسار الراوتر، مع تحديث يدوي وتخزين مؤقت قصير لا يُعرض على أنه اتصال حي. كل عملية كتابة تُسجل بنتيجة نجاح أو فشل واضحة وتعيد قراءة المورد بعد النجاح.

## حدود التشغيل الفعلي

يتطلب التشغيل الواقعي وسيطًا قابلًا للوصول من الهاتف، ومُعدًا للوصول إلى الراوتر محليًا. للوصول الخارجي يجب استخدام VPN أو قناة HTTPS آمنة إلى الوسيط، وعدم نشر منفذ RouterOS API غير المشفر على الإنترنت. يجب أن يملك حساب RouterOS أقل صلاحيات لازمة لكل عملية.
