export type { MndpDevice as DiscoveredMikroTik } from "./mndp-parser";
import type { MndpDevice as DiscoveredMikroTik } from "./mndp-parser";

/** يختار Metro ملف Android الأصلي في APK وبديل الويب في المعاينة. */
export async function discoverMikroTik(): Promise<DiscoveredMikroTik[]> {
  return [];
}
